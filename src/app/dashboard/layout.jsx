export const metadata = {
    title: "Dashboard | NEXT JS + Tailwind CSS",
    description:
        "This is starter template of NEXT JS and Tailwind CSS project.",
};

export default function DashLayout({ children }) {
    return <div>{children}</div>;
}
